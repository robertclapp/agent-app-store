# Development and test dependencies
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# tests/test_health.py parses docker-compose.yml. Declared here rather than
# relying on it arriving transitively through uvicorn[standard].
pyyaml>=6.0
pyright>=1.1.400,<2
